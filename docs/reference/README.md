# Reference material

## `ui-reference.png` — **still not in the repo**

`implementation.md` names `docs/reference/ui-reference.png` as the visual
reference for the app (the 4-screen mockup: Profile, Vedic Calendar, Pooja
Preparation, Chant Player). The file was not supplied with the plan, so it
could not be copied here in Phase 0, and it had still not arrived when Phase 1
closed.

**It blocks the side-by-side half of the Phase 1 acceptance.** Everything else
that check needs is done: `docs/screens/p1/` holds the app's own screenshots of
the four screens at three widths, ready to sit next to the mockup. When the
file lands, drop it here and compare — structure, palette and hierarchy only.

Remember §3.1 when it arrives: the mockup is a _visual_ reference only. Its
text is North-Indian/Hindi placeholder content ("Aarav Sharma", "Shukle
Paksha"). Copy layout, palette, components and hierarchy — never the text.
