{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "ePooja pure TS library (no React Native / Expo imports)",
  "extends": "./base.json",
  "compilerOptions": {
    "types": ["node"],
    "noEmit": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
